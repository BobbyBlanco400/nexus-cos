# Nexus COS Environment Configuration
# Generated by GitHub Copilot Code Agent PF Review & Fix Script

# Application Settings
NODE_ENV=development
APP_NAME=nexus-cos
APP_VERSION=1.0.0
APP_PORT=8080

# Authentication Service
AUTH_SERVICE_PORT=3100
AUTH_SERVICE_URL=http://localhost:3100
JWT_SECRET=nexus-cos-secret-key-2024-secure
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Billing Service  
BILLING_SERVICE_PORT=3110
BILLING_SERVICE_URL=http://localhost:3110

# Database Configuration
# IMPORTANT: Update DB_HOST with the actual database host
# Options:
#   - localhost (if PostgreSQL is on the same server)
#   - nexus-cos-postgres (if using Docker container as per PF_ARCHITECTURE.md)
#   - Your actual DB server IP/hostname (e.g., 192.168.1.100 or db.example.com)
# Current setting 'localhost' will work if PostgreSQL is installed locally
DATABASE_URL=postgresql://nexuscos:password@localhost:5432/nexuscos_db
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nexuscos_db
DB_USER=nexuscos
DB_PASSWORD=password

# SSL/TLS Configuration
SSL_ENABLED=true
SSL_CERT_PATH=/etc/letsencrypt/live/nexuscos/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/nexuscos/privkey.pem
SSL_EMAIL=admin@nexuscos.online

# Nginx Configuration
NGINX_HOST=nexuscos.online
NGINX_PORT=80
NGINX_SSL_PORT=443

# Logging
LOG_LEVEL=info
LOG_DIR=/home/runner/work/nexus-cos/nexus-cos/logs

# PM2 Configuration
PM2_HOME=/home/runner/.pm2
PM2_INSTANCES=1

# Development Settings
DEBUG=nexus:*
VERBOSE=true

# OAuth Configuration (required for docker-compose.pf.yml)
OAUTH_CLIENT_ID=nexus-cos-client-2024
OAUTH_CLIENT_SECRET=nexus-cos-secret-key-oauth-2024-secure