# Nexus COS Backend Requirements - TRAE Solo Compatible
# Core FastAPI application dependencies
fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7

# Database dependencies
sqlalchemy==2.0.43
psycopg2-binary==2.9.10
alembic==1.16.4

# Configuration and utilities
python-dotenv==1.1.1
PyYAML==6.0.1

# TRAE Solo orchestration dependencies
pydantic-settings==2.1.0
httpx==0.26.0
aiofiles==24.1.0

# Updated docker dependencies with PyYAML 6.0+ support
# Note: Removed docker-compose==1.29.2 as it requires PyYAML<6 (incompatible with Python 3.10+)
# If Docker Compose functionality is needed, use Docker Compose V2 CLI or docker package directly
docker==7.1.0

# Additional useful dependencies
requests==2.31.0
click==8.1.6
cryptography==41.0.7

# Health check and monitoring for TRAE Solo
prometheus-client==0.20.0
healthcheck==1.3.3
