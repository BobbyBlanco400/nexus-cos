apiVersion: v1
kind: Service
metadata:
  name: backend-api
  namespace: nexus-platform
  labels:
    app: backend-api
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: backend-api
