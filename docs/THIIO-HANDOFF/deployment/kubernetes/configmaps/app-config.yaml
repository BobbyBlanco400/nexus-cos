apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: nexus-platform
data:
  LOG_LEVEL: "info"
  NODE_ENV: "production"
  API_VERSION: "v2"
  ENABLE_METRICS: "true"
  ENABLE_TRACING: "true"
